{% set value = (context and context['questions'] is defined and context['questions'][question.id]['answer'] is defined) ? context['questions'][question.id]['answer'] : '' %}
{%- if value.n is defined -%}
    {{- getAnswer(value.n) -}}
{%- else -%}
    {{- value -}}
{%- endif -%}