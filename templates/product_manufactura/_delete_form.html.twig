<form method="post" action="{{ path('app_product-manufactura_delete', {'id': productManufactura.id}) }}" onsubmit="return confirm('{{ 'Hersteller lschen?'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productManufactura.id) }}">
    <button class="btn btn-danger btn-sm">{{ 'w.delete'|trans }}</button>
</form>
