{% extends 'base.html.twig' %}

{% block title %}{{ 'teams.edit'|trans }}{% endblock %}

{% block main %}
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title">{{ 'teams.edit'|trans }}</h4>
            </div>
            <div class="card-body">
                {{ include('project_team/_form.html.twig', {'button_label': 'Update'}) }}
{#                {{ include('project_team/_delete_form.html.twig') }}#}
            </div>
        </div>
    </div>
</div>

    <div class="row">
        <div class="col-md-12 text-center">
            {{ include('project_team/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
