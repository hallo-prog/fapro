<form method="post" action="{{ path('app_product_order_delete', {'id': product_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_order.id) }}">
    <button class="btn">Delete</button>
</form>
