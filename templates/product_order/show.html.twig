{% extends 'base.html.twig' %}

{% block title %}ProductOrder{% endblock %}

{% block body %}
    <h1>ProductOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_order.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_order.name }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ product_order.amount }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product_order.price }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ product_order.createdAt ? product_order.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_order_index') }}">back to list</a>

    <a href="{{ path('app_product_order_edit', {'id': product_order.id}) }}">edit</a>

    {{ include('product_order/_delete_form.html.twig') }}
{% endblock %}
