{% if (done is not defined or done == false) %}
{#    <div class="progress">#}
{#        <div class="progress-bar" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>#}
{#    </div>#}
    <h2 style="text-align: center;vertical-align: inherit;">An wen können wir uns wenden?</h2>
    {% if errors is defined %}
        <div class="last-conv" style="margin: 0 auto;width: fit-content;padding: 0 0 30px;">
        <div class="alert alert-with-icon">
            {% for k, er in errors %}
                {% if er.message is defined %}
                    {{ er.message|raw }}
                {% else %}
                    {{ er|raw }}
                {% endif %}
            {% endfor %}
        </div>
        </div>
    {% endif %}
    <div class="last-conv" style="margin: 0 auto;width: fit-content;padding-bottom: 24px;">
        <div class="row">
            <div class="colp col-4">
                <div class="form-group bmd-form-group">
{#                    {{ form_label(form.customer.sex) }}#}
                    {{ form_widget(form.customer.sex, {attr:{'style':'padding:0'}}) }}
                    {{ form_errors(form.customer.sex) }}
                </div>
            </div>
            <div class="colp col-8">
                <div class="form-group bmd-form-group">
{#                    {{ form_label(form.customer.name) }}#}
                    {{ form_widget(form.customer.name) }}
                    {{ form_errors(form.customer.name) }}
                    <small id="name_err" style="display:none;color: #c25a5a">Der Name darf nicht leer sein</small>
                </div>
            </div>
{#            <div class="colp col-5">#}
{#                <div class="form-group bmd-form-group">#}
{#                    {{ form_label(form.customer.surName) }}#}
{#                    {{ form_widget(form.customer.surName) }}#}
{#                    {{ form_errors(form.customer.surName) }}#}
{#                </div>#}
{#            </div>#}
            {# <div class="col-md-3"> #}
            {# <div class="form-group bmd-form-group"> #}
            {# {{ form_label(form.customer.companyName, 'Firmenname') }} #}
            {# {{ form_widget(form.customer.companyName) }} #}
            {# </div> #}
            {# </div> #}
        </div>
        <div class="row">
            <div class="colp col-6">
                <div class="form-group bmd-form-group">
                    {{ form_widget(form.customer.email) }}
                    {{ form_errors(form.customer.email) }}
                    <small id="email_err" style="display:none;color: #c25a5a">Fehlerhafte E-Mail Adresse</small>
                </div>
            </div>
            <div class="colp col-6">
                <div class="form-group bmd-form-group">
                    {{ form_widget(form.customer.phone) }}
                    {{ form_errors(form.customer.phone) }}
                </div>
            </div>
        </div>
        <p style="text-align: center;vertical-align: inherit;margin-bottom: 10px;margin-top: 16px;">Wo wird Ihre Anlage installiert?</p>
{#        <div class="row">#}
{#            <div class="colp col-12">#}
{#                <div class="form-group bmd-form-group">#}
{#                    <select required class="form-control SearchPlaces js-example-basic-single js-states form-control" style="padding:10px;width: 100%" name="city"></select>#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
        <div class="row">
            <div class="colp col-4">
                <div class="form-group bmd-form-group">
                    {{ form_widget(form.customer.zip) }}
                </div>
            </div>
            <div class="colp col-8">
                <div class="form-group bmd-form-group">
                    {{ form_widget(form.customer.city) }}
                </div>
            </div>
        </div>
{#        {% if link is defined and link is not null %}#}
            <div class="row">
                <div class="colp col-12">
                    <div class="form-group bmd-form-group">
                        <label class="bmd-label form-label required" for="inquiry_customer_name">Rabattcode</label>
                        <input type="text" id="key" name="key" value="{% if link is defined and link is not null %} {{ link.link }}{% endif %}" required="required" class="last-form-control form-control">
                    </div>
                </div>
            </div>
{#        {% endif %}#}
        {# <div class="col-4"> #}
        {# <div class="form-group bmd-form-group"> #}
        {# {{ form_label(form.customer.country) }} #}
        {# {{ form_widget(form.customer.country) }} #}
        {# </div> #}
        {# </div> #}
    </div>
{% else %}
    <h2 style="text-align: center;vertical-align: inherit;">Viele Dank! Ihre Anfrage wurde erfolgreich gesendet.</h2>
    <div class="last-conv" style="margin: 0 auto;width: fit-content;padding-bottom: 24px;">
        <p style="text-align: center;font-size: 0.9rem">
            Ein Mitarbeiter unseres Teams wird sich in Kürze mit Ihnen in Verbindung setzen.<br>
        </p>
{#        <div class="responsive-video">#}
{#            <iframe width="325" height="249" src="{{ asset('assets/zoe-sagt-danke.mp4') }}" frameborder="0" allowfullscreen></iframe>#}
{#        </div>#}
    </div>
{% endif %}