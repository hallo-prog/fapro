{{ form_start(form) }}
<div class="form-group row">
    {{ form_label(form.name) }}
    <div class="col-sm-8">
        {{ form_widget(form.name) }}
    </div>
</div>
<div class="form-group row">
    <div class="col-sm-8 offset-sm-4">
        <button class="btn btn-primary">{{ 'f.button.saveName'|trans({'%name%':'w.productCategory'|trans}) }}</button>
    </div>
</div>
{{ form_end(form) }}
{% if product_category is defined and product_category.id %}
    {{ include('backend/product_category/_delete_form.html.twig') }}
{% endif %}